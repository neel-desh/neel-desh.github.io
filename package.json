{
  "name": "neeldeshmukh.com",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/build-markdown.mjs && npm run check:visibility && npm run check:pii",
    "check:visibility": "node scripts/check-visibility.mjs",
    "check:pii": "node scripts/check-pii.mjs --self-test && node scripts/check-pii.mjs",
    "preview": "astro preview",
    "check": "astro check",
    "test": "node --test scripts/*.test.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^5.2.4"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "turndown": "^7.2.4",
    "typescript": "^5.7.3"
  }
}
